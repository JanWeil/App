//Stateless widget?

import 'package:flutter/material.dart';

//!maybe StatefulWidget?
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext cotext) {
    return Container(
      child: Text('home'),
    );
  }
}